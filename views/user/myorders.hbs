<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Orders | Elegance</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-black: #000000;
            --primary-white: #ffffff;
            --light-gray: #f8f9fa;
            --medium-gray: #e9ecef;
            --dark-gray: #495057;
            --border-gray: #dee2e6;
            --hover-gray: #f5f5f5;
            --shadow: rgba(0, 0, 0, 0.1);
            --text-muted: #6c757d;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background-color: var(--primary-white);
            color: var(--primary-black);
            line-height: 1.6;
        }

        /* Page Header */
        .page-header {
            background: linear-gradient(135deg, var(--primary-black) 0%, var(--dark-gray) 100%);
            color: var(--primary-white);
            padding: 80px 0 60px;
            position: relative;
            overflow: hidden;
        }

        .page-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1000 100" fill="rgba(255,255,255,0.05)"><polygon points="0,0 1000,0 1000,100 0,80"/></svg>');
            background-size: cover;
        }

        .page-header .container {
            position: relative;
            z-index: 2;
            text-align: center;
        }

        .page-title {
            font-family: 'Playfair Display', serif;
            font-size: 3.5rem;
            font-weight: 700;
            margin-bottom: 15px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }

        .page-subtitle {
            font-size: 1.2rem;
            margin-bottom: 30px;
            opacity: 0.9;
        }

        .header-actions {
            display: flex;
            justify-content: center;
            gap: 15px;
            flex-wrap: wrap;
        }

        .header-btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 12px 25px;
            text-decoration: none;
            border-radius: 50px;
            font-weight: 500;
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }

        .header-btn.btn-primary {
            background-color: var(--primary-white);
            color: var(--primary-black);
            border-color: var(--primary-white);
        }

        .header-btn.btn-primary:hover {
            background-color: var(--light-gray);
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

        .header-btn.btn-outline-white {
            background-color: transparent;
            color: var(--primary-white);
            border-color: var(--primary-white);
        }

        .header-btn.btn-outline-white:hover {
            background-color: var(--primary-white);
            color: var(--primary-black);
        }

        /* Orders Container */
        .orders-container {
            max-width: 1200px;
            margin: -30px auto 0;
            padding: 0 20px 50px;
            position: relative;
            z-index: 3;
        }

        /* Filter Section */
        .filter-section {
            background: var(--primary-white);
            border-radius: 15px;
            padding: 25px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px var(--shadow);
            display: flex;
            gap: 20px;
            align-items: center;
            flex-wrap: wrap;
            border: 1px solid var(--border-gray);
        }

        .filter-group {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .filter-label {
            font-weight: 600;
            color: var(--primary-black);
            white-space: nowrap;
        }

        .filter-select, .search-input {
            padding: 10px 15px;
            border: 2px solid var(--border-gray);
            border-radius: 8px;
            font-size: 14px;
            background-color: var(--primary-white);
            color: var(--primary-black);
            transition: all 0.3s ease;
        }

        .filter-select:focus, .search-input:focus {
            outline: none;
            border-color: var(--primary-black);
            box-shadow: 0 0 0 3px rgba(0,0,0,0.1);
        }

        .search-input {
            min-width: 250px;
        }

        .search-btn {
            padding: 10px 20px;
            background-color: var(--primary-black);
            color: var(--primary-white);
            border: none;
            border-radius: 8px;
            font-weight: 500;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .search-btn:hover {
            background-color: var(--dark-gray);
            transform: translateY(-1px);
        }

        /* Order Cards */
        .order-card {
            background: var(--primary-white);
            border-radius: 15px;
            margin-bottom: 25px;
            box-shadow: 0 5px 20px var(--shadow);
            overflow: hidden;
            transition: all 0.3s ease;
            animation: slideInUp 0.6s ease forwards;
            opacity: 0;
            transform: translateY(30px);
            border: 1px solid var(--border-gray);
        }

        @keyframes slideInUp {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .order-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0,0,0,0.15);
        }

        .order-header {
            background: linear-gradient(135deg, var(--light-gray) 0%, var(--medium-gray) 100%);
            padding: 20px 25px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 10px;
            border-bottom: 1px solid var(--border-gray);
        }

        .order-number {
            font-weight: 600;
            color: var(--primary-black);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .order-date {
            color: var(--text-muted);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .order-body {
            padding: 25px;
        }

        .order-details {
            margin-bottom: 20px;
        }

        .product-name {
            font-size: 1.4rem;
            font-weight: 600;
            margin-bottom: 20px;
            color: var(--primary-black);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .detail-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
        }

        .detail-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 12px 0;
            border-bottom: 1px solid var(--medium-gray);
        }

        .detail-label {
            font-weight: 500;
            color: var(--text-muted);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .detail-value {
            font-weight: 600;
            color: var(--primary-black);
        }

        .status-badge {
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            display: inline-flex;
            align-items: center;
            gap: 5px;
            background-color: var(--primary-black);
            color: var(--primary-white);
        }

        .order-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 15px;
            padding-top: 20px;
            border-top: 1px solid var(--medium-gray);
        }

        .order-total {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--primary-black);
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .order-actions {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }

        .action-btn {
            padding: 8px 16px;
            text-decoration: none;
            border-radius: 25px;
            font-size: 14px;
            font-weight: 500;
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 6px;
            border: 2px solid transparent;
        }

        .action-btn.btn-primary {
            background-color: var(--primary-black);
            color: var(--primary-white);
            border-color: var(--primary-black);
        }

        .action-btn.btn-primary:hover {
            background-color: var(--dark-gray);
            transform: translateY(-1px);
        }

        .action-btn.btn-outline {
            background-color: transparent;
            color: var(--primary-black);
            border-color: var(--primary-black);
        }

        .action-btn.btn-outline:hover {
            background-color: var(--primary-black);
            color: var(--primary-white);
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 80px 20px;
            background: var(--primary-white);
            border-radius: 15px;
            box-shadow: 0 5px 20px var(--shadow);
            border: 1px solid var(--border-gray);
        }

        .empty-icon {
            font-size: 5rem;
            color: var(--text-muted);
            margin-bottom: 25px;
        }

        .empty-title {
            font-size: 2rem;
            font-weight: 600;
            margin-bottom: 15px;
            color: var(--primary-black);
        }

        .empty-text {
            font-size: 1.1rem;
            color: var(--text-muted);
            margin-bottom: 30px;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
        }

        .shop-btn {
            display: inline-flex;
            align-items: center;
            gap: 10px;
            padding: 15px 30px;
            background-color: var(--primary-black);
            color: var(--primary-white);
            text-decoration: none;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }

        .shop-btn:hover {
            background-color: var(--dark-gray);
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.2);
            color: var(--primary-white);
        }

        /* Footer */
        .footer {
            background: linear-gradient(135deg, var(--primary-black) 0%, var(--dark-gray) 100%);
            color: var(--primary-white);
            padding: 50px 0 20px;
            margin-top: 50px;
        }

        .footer-content {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        .footer-title {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            font-weight: 600;
            margin-bottom: 20px;
            color: var(--primary-white);
        }

        .footer-text {
            margin-bottom: 25px;
            opacity: 0.9;
            line-height: 1.7;
        }

        .social-links {
            display: flex;
            gap: 15px;
        }

        .social-link {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            background-color: rgba(255,255,255,0.1);
            border-radius: 50%;
            color: var(--primary-white);
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .social-link:hover {
            background-color: var(--primary-white);
            color: var(--primary-black);
            transform: translateY(-2px);
        }

        .footer-links {
            list-style: none;
        }

        .footer-links li {
            margin-bottom: 12px;
        }

        .footer-links a {
            color: var(--primary-white);
            text-decoration: none;
            opacity: 0.9;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .footer-links a:hover {
            opacity: 1;
            padding-left: 5px;
        }

        .copyright {
            text-align: center;
            padding-top: 30px;
            margin-top: 40px;
            border-top: 1px solid rgba(255,255,255,0.2);
            opacity: 0.8;
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            .page-title {
                font-size: 2.5rem;
            }
            
            .filter-section {
                flex-direction: column;
                align-items: stretch;
            }
            
            .filter-group {
                flex-direction: column;
                align-items: stretch;
            }
            
            .search-input {
                min-width: auto;
                width: 100%;
            }
            
            .detail-grid {
                grid-template-columns: 1fr;
            }
            
            .order-footer {
                flex-direction: column;
                align-items: stretch;
                text-align: center;
            }
            
            .order-actions {
                justify-content: center;
            }
        }
    </style>
</head>
<body>
    <!-- Page Header -->
    <div class="page-header">
        <div class="container">
            <h1 class="page-title">My Orders</h1>
            <p class="page-subtitle">Review your purchase history and track your orders</p>
            <div class="header-actions">
                <a href="/" class="header-btn btn-primary">
                    <i class="fas fa-store"></i> Continue Shopping
                </a>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="orders-container">
        <!-- Filter Section -->
        <div class="filter-section">
            <div class="filter-group">
                <span class="filter-label"><i class="fas fa-filter"></i> Filter by:</span>
                <select class="filter-select">
                    <option value="all">All Orders</option>
                    <option value="pending">Pending</option>
                    <option value="shipped">Shipped</option>
                    <option value="delivered">Delivered</option>
                    <option value="cancelled">Cancelled</option>
                </select>
            </div>
            
            <div class="filter-group" style="flex-grow: 1;">
                <input type="text" class="search-input" placeholder="Search by product name...">
                <button class="search-btn"><i class="fas fa-search"></i> Search</button>
            </div>
        </div>

        <!-- Orders List -->
        {{#if products}}
        <div id="orders-list">
            {{#each products}}
            <div class="order-card">
                <div class="order-header">
                    <div class="order-number">
                        <i class="fas fa-receipt"></i> Order #{{this._id}}
                    </div>
                    <div class="order-date">
                        <i class="far fa-calendar-alt"></i> {{this.date}}
                    </div>
                </div>
                
                <div class="order-body">
                    <div class="row g-0">
                        <div class="col-md-12">
                            <div class="order-details">
                                <h5 class="product-name">
                                    <i class="fas fa-box-open"></i> {{this.item.productName}}
                                </h5>
                                
                                <div class="detail-grid">
                                    <div class="detail-item">
                                        <span class="detail-label"><i class="fas fa-hashtag"></i> Quantity</span>
                                        <span class="detail-value">{{this.quantity}}</span>
                                    </div>
                                    
                                    <div class="detail-item">
                                        <span class="detail-label"><i class="fas fa-tag"></i> Price</span>
                                        <span class="detail-value">₹{{this.item.mrp}}</span>
                                    </div>
                                    
                                    <div class="detail-item">
                                        <span class="detail-label"><i class="fas fa-truck"></i> Delivery</span>
                                        <span class="detail-value">Standard (3-5 days)</span>
                                    </div>
                                    
                                    <div class="detail-item">
                                        <span class="detail-label"><i class="fas fa-info-circle"></i> Status</span>
                                        <span class="status-badge status-{{this.status}}">
                                            <i class="fas fa-circle-notch"></i> {{this.status}}
                                        </span>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="order-footer">
                                <div class="order-total">
                                    <i class="fas fa-wallet"></i> Total: ₹{{this.item.mrp}}
                                </div>
                                <div class="order-actions">
                                    <a href="#" class="action-btn btn-primary">
                                        <i class="fas fa-map-marker-alt"></i> Track Order
                                    </a>
                                    <a href="/order/details/{{this._id}}" class="action-btn btn-outline">
                                        <i class="fas fa-file-alt"></i> View Details
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            {{/each}}
        </div>
        {{else}}
        <!-- Empty State -->
        <div class="empty-state">
            <div class="empty-icon">
                <i class="fas fa-shopping-bag"></i>
            </div>
            <h3 class="empty-title">No Orders Yet</h3>
            <p class="empty-text">
                You haven't placed any orders with us yet. Browse our collection and discover something special for your wardrobe!
            </p>
            <a href="/" class="shop-btn">
                <i class="fas fa-store"></i> Start Shopping
            </a>
        </div>
        {{/if}}
    </div>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-content">
            <div class="row">
                <div class="col-md-4 mb-4 mb-md-0">
                    <h3 class="footer-title">Elegance</h3>
                    <p class="footer-text">
                        Premium clothing and accessories crafted with care and attention to detail. We bring you the finest fashion from around the world.
                    </p>
                    <div class="social-links">
                        <a href="#" class="social-link"><i class="fab fa-facebook-f"></i></a>
                        <a href="#" class="social-link"><i class="fab fa-twitter"></i></a>
                        <a href="#" class="social-link"><i class="fab fa-instagram"></i></a>
                        <a href="#" class="social-link"><i class="fab fa-pinterest"></i></a>
                    </div>
                </div>
                <div class="col-md-4 mb-4 mb-md-0">
                    <h4 class="footer-title">Quick Links</h4>
                    <ul class="footer-links">
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Home</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Shop</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> About Us</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Contact</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Blog</a></li>
                    </ul>
                </div>
                <div class="col-md-4">
                    <h4 class="footer-title">Customer Service</h4>
                    <ul class="footer-links">
                        <li><a href="#"><i class="fas fa-chevron-right"></i> My Account</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Order Tracking</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Returns & Exchanges</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> Shipping Info</a></li>
                        <li><a href="#"><i class="fas fa-chevron-right"></i> FAQ</a></li>
                    </ul>
                </div>
            </div>
            <div class="copyright">
                &copy; 2025 Elegance. All rights reserved. | Designed with <i class="fas fa-heart"></i> for fashion lovers
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Animation for order cards
        document.addEventListener('DOMContentLoaded', function() {
            const orderCards = document.querySelectorAll('.order-card');
            orderCards.forEach((card, index) => {
                card.style.animationDelay = `${index * 0.1}s`;
            });
            
            // Filter functionality
            const filterSelect = document.querySelector('.filter-select');
            filterSelect.addEventListener('change', function() {
                const status = this.value;
                const orderCards = document.querySelectorAll('.order-card');
                
                orderCards.forEach(card => {
                    const statusBadge = card.querySelector('.status-badge');
                    const cardStatus = statusBadge.textContent.toLowerCase().trim();
                    
                    if (status === 'all' || cardStatus.includes(status)) {
                        card.style.display = 'block';
                    } else {
                        card.style.display = 'none';
                    }
                });
            });
            
            // Search functionality
            const searchInput = document.querySelector('.search-input');
            const searchBtn = document.querySelector('.search-btn');
            
            searchBtn.addEventListener('click', performSearch);
            searchInput.addEventListener('keypress', function(e) {
                if (e.key === 'Enter') performSearch();
            });
            
            function performSearch() {
                const query = searchInput.value.toLowerCase();
                const productNames = document.querySelectorAll('.product-name');
                
                productNames.forEach(nameEl => {
                    const card = nameEl.closest('.order-card');
                    const productName = nameEl.textContent.toLowerCase();
                    
                    if (query === '' || productName.includes(query)) {
                        card.style.display = 'block';
                    } else {
                        card.style.display = 'none';
                    }
                });
            }
        });
    </script>
</body>
</html>